import {
  initializeAnimation,
  animateExample,
  animateLeft,
  animateRight
} from '../src/shared/animations';

document.addEventListener('DOMContentLoaded', () => {
  initializeAnimation(); animateExample(); animateLeft(); animateRight();
});
